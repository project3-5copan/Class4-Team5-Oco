import * as PropTypes from "prop-types";
import * as React from "react";
export class AlternateDataSeries extends React.Component {
    getChildContext() {
        const { data } = this.props;
        const { plotData, xAccessor } = this.context;
        const startDate = xAccessor(plotData[0]);
        const endDate = xAccessor(plotData[plotData.length - 1]);
        return {
            plotData: data.filter((d) => {
                const date = xAccessor(d);
                return date > startDate && date < endDate;
            }),
        };
    }
    render() {
        return this.props.children;
    }
}
AlternateDataSeries.contextTypes = {
    plotData: PropTypes.array,
    xAccessor: PropTypes.func.isRequired,
};
AlternateDataSeries.childContextTypes = {
    plotData: PropTypes.array,
};
//# sourceMappingURL=AlternateDataSeries.js.map