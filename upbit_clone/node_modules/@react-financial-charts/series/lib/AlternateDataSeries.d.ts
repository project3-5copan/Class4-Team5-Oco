import * as PropTypes from "prop-types";
import * as React from "react";
export interface AlternateDataSeriesProps<TData> {
    readonly data: TData[];
}
export declare class AlternateDataSeries<TData> extends React.Component<AlternateDataSeriesProps<TData>> {
    static contextTypes: {
        plotData: PropTypes.Requireable<any[]>;
        xAccessor: PropTypes.Validator<(...args: any[]) => any>;
    };
    static childContextTypes: {
        plotData: PropTypes.Requireable<any[]>;
    };
    getChildContext(): {
        plotData: TData[];
    };
    render(): React.ReactNode;
}
